# Backend target for the Vite dev-server proxy (local FastAPI by default).
# In a GitHub Codespace you can instead point this at the forwarded URL, e.g.
# VITE_API_TARGET=https://your-codespace-8000.app.github.dev
VITE_API_TARGET=http://localhost:8000
